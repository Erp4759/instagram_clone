import 'package:flutter/material.dart';

/// Placeholder widget to keep any accidental imports from this file
/// from breaking the build. The canonical profile UI lives in
/// `profile_screen.dart`.
class ProfileScreenFixedPlaceholder extends StatelessWidget {
  const ProfileScreenFixedPlaceholder({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
